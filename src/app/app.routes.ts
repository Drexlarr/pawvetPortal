import { Routes } from '@angular/router';
import { AppointmentsComponent } from './routes/components/appointments/appointments.component';

export const routes: Routes = [
    {path: 'appointments', component: AppointmentsComponent}
];
